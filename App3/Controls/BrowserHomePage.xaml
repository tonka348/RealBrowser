<UserControl
    x:Class="App3.Controls.BrowserHomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <Style x:Key="QuickLinkButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="140"/>
            <Setter Property="Height" Value="110"/>
            <Setter Property="Margin" Value="8"/>
        </Style>
        
        <!-- Custom style for AutoSuggestBox with acrylic (mica-like) effect -->
        <Style x:Key="AcrylicAutoSuggestBoxStyle" TargetType="AutoSuggestBox">
            <Setter Property="Background">
                <Setter.Value>
                    <AcrylicBrush
                        TintColor="#CCFFFFFF"
                        TintOpacity="0.6"
                        FallbackColor="#F3F3F3"/>
                </Setter.Value>
            </Setter>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,0,8,0"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Logo and Title -->
        <StackPanel Grid.Row="0" Margin="0,100,0,20" HorizontalAlignment="Center">
            <TextBlock Text="My Browser" FontSize="48" FontWeight="SemiBold" HorizontalAlignment="Center"/>
            <TextBlock Text="Powered by WinUI 3" FontSize="16" Opacity="0.7" HorizontalAlignment="Center" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Search Box with acrylic effect -->
        <AutoSuggestBox x:Name="SearchBox" 
                       Grid.Row="1"
                       PlaceholderText="Search or enter address"
                       QueryIcon="Find"
                       Width="600"
                       Height="44"
                       Margin="0,20,0,30"
                       HorizontalAlignment="Center"
                       QuerySubmitted="SearchBox_QuerySubmitted"
                       TextChanged="SearchBox_TextChanged"
                       SuggestionChosen="SearchBox_SuggestionChosen"
                       KeyDown="SearchBox_KeyDown"
                       Style="{StaticResource AcrylicAutoSuggestBoxStyle}">
            <AutoSuggestBox.Shadow>
                <ThemeShadow />
            </AutoSuggestBox.Shadow>
        </AutoSuggestBox>

        <!-- Quick links -->
        <ItemsControl Grid.Row="2" HorizontalAlignment="Center">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid MaximumRowsOrColumns="4" Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.Items>
                <Button Content="Google" Tag="https://www.google.com" Click="QuickLinkButton_Click" Style="{StaticResource QuickLinkButtonStyle}"/>
                <Button Content="YouTube" Tag="https://www.youtube.com" Click="QuickLinkButton_Click" Style="{StaticResource QuickLinkButtonStyle}"/>
                <Button Content="GitHub" Tag="https://www.github.com" Click="QuickLinkButton_Click" Style="{StaticResource QuickLinkButtonStyle}"/>
                <Button Content="Wikipedia" Tag="https://www.wikipedia.org" Click="QuickLinkButton_Click" Style="{StaticResource QuickLinkButtonStyle}"/>
            </ItemsControl.Items>
        </ItemsControl>

        <!-- Button to refresh wallpaper -->
        <Button x:Name="RefreshWallpaperButton"
                Grid.Row="1"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Margin="0,0,20,20"
                Click="RefreshWallpaperButton_Click"
                ToolTipService.ToolTip="Refresh wallpaper">
            <SymbolIcon Symbol="Refresh"/>
        </Button>
    </Grid>
</UserControl>